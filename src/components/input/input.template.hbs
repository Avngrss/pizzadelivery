<label>
  <p>{{label}}</p>
  <input
    label="{{label}}"
    name="{{name}}"
    type="{{type}}"
    placeholder="{{placeholder}}"
    required="{{required}}"
    class="block w-full border-2 border-yellow-200 rounded {{className}}"
  />
</label>