<label>
  <p>{{label}}</p>
  <input
    label="{{label}}"
    name="{{name}}"
    type="{{type}}"
    placeholder="{{placeholder}}"
    class="block w-full border-2 border-indigo-500 rounded"
  />
</label>